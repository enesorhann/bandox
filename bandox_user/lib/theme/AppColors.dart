import 'package:flutter/material.dart';

class AppColors {
  // Temel Renkler (Soft Mavi Teması)
  static const Color primary = Color(0xFF2563EB); // Indigo-600 (Apple vibe)
  static const Color onPrimary = Colors.white;

  static const Color secondary = Color(0xFF60A5FA); // Soft Light Blue
  static const Color onSecondary = Colors.white;

  //


  static const Color background = Color(0xFFF9FAFB); // Çok açık gri (nötr iOS stili)
  static const Color onBackground = Color(0xFF111827); // Dark Grey for text

  static const Color surface = Colors.white; // Kart zeminleri
  static const Color onSurface = Color(0xFF1F2937); // Başlık/metinler için koyu gri

  static const Color error = Color(0xFFEF4444); // Apple kırmızısına benzer
  static const Color onError = Colors.white;

  static const Color border = Color(0xFFE5E7EB); // Kart, input kenarlıkları

  // Opsiyonel nötr griler
  static const Color gray = Color(0xFF6B7280); // İkincil metin rengi
  static const Color lightGray = Color(0xFFF3F4F6);
  static const Color darkGray = Color(0xFF374151);

  // Başlıklar ve metinler için
  static const Color text = Color(0xFF1E293B);
}
